{
  "collectionName": "components_global_main_social_urls",
  "info": {
    "name": "main_social_urls",
    "icon": "home"
  },
  "options": {},
  "attributes": {
    "twitter": {
      "type": "string",
      "regex": "^((?!twitter).)*$"
    },
    "instagram": {
      "type": "string",
      "regex": "^((?!instagram).)*$"
    },
    "facebook": {
      "type": "string",
      "regex": "^((?!facebook).)*$"
    },
    "youtube": {
      "type": "string",
      "regex": "^((?!youtube).)*$"
    },
    "linkedin": {
      "type": "string",
      "regex": "^((?!linkedin).)*$"
    },
    "contact_email": {
      "type": "email"
    }
  }
}
